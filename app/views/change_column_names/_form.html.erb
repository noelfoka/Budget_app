<%= form_with(model: change_column_name) do |form| %>
  <% if change_column_name.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(change_column_name.errors.count, "error") %> prohibited this change_column_name from being saved:</h2>

      <ul>
        <% change_column_name.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
